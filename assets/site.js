const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav-links');toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});
document.querySelectorAll('.filter').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.setAttribute('aria-pressed','false'));button.setAttribute('aria-pressed','true');const value=button.dataset.filter;document.querySelectorAll('.work-item').forEach(item=>item.hidden=value!=='all'&&item.dataset.category!==value)}));
const role=document.querySelector('#clientRole');const professionalFields=document.querySelectorAll('.professional-fields');function updateRole(){const show=['perit','developer','contractor'].includes(role?.value);professionalFields.forEach(field=>field.hidden=!show)}role?.addEventListener('change',updateRole);updateRole();
const labels={clientRole:'Client type',name:'Name',company:'Company',phone:'Contact number',email:'Email',service:'Service',locality:'Locality',timing:'Desired timing',accessWidth:'Access width',dimensions:'Dimensions / depth',occupiedSite:'Occupied site',quotationDeadline:'Quotation deadline',drawingRevision:'Drawing revision',projectContact:'Project contact',siteMeeting:'Site meeting',description:'Project description'};
const quote=document.querySelector('#quote-form');quote?.addEventListener('submit',event=>{event.preventDefault();if(!quote.reportValidity())return;const data=new FormData(quote);const isProfessional=['perit','developer','contractor'].includes(data.get('clientRole'));const lines=['Hello Trident Group Solutions.','',isProfessional?'PROFESSIONAL PROJECT ENQUIRY':'QUICK PROJECT ENQUIRY'];for(const [key,label] of Object.entries(labels)){const value=String(data.get(key)||'').trim();if(!value||(!isProfessional&&['company','quotationDeadline','drawingRevision','projectContact','siteMeeting'].includes(key)))continue;lines.push(`${label}: ${value}`)}lines.push('','I will now attach available site photos, video, drawings or BOQ.');const url=`https://wa.me/35679990305?text=${encodeURIComponent(lines.join('\n'))}`;const result=document.querySelector('#form-result');if(result){result.hidden=false;result.innerHTML='<strong>WhatsApp is opening.</strong><br>Review the prepared details, attach your photos or documents, then press Send. Your enquiry is not received until you send the WhatsApp message.'}window.location.href=url});
const banner=document.querySelector('.cookie-banner');if(localStorage.getItem('trident-cookie-choice'))banner?.remove();document.querySelectorAll('[data-cookie]').forEach(button=>button.addEventListener('click',()=>{localStorage.setItem('trident-cookie-choice',button.dataset.cookie);banner?.remove()}));