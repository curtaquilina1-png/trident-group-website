const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.nav-links');toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});
document.querySelectorAll('.filter').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(b=>b.setAttribute('aria-pressed','false'));button.setAttribute('aria-pressed','true');const value=button.dataset.filter;document.querySelectorAll('.work-item').forEach(item=>item.hidden=value!=='all'&&item.dataset.category!==value)}));
const role=document.querySelector('#clientRole');const professional=document.querySelector('.professional-fields');function updateRole(){if(!role||!professional)return;professional.hidden=!['perit','developer','contractor'].includes(role.value)}role?.addEventListener('change',updateRole);updateRole();
const quote=document.querySelector('#quote-form');quote?.addEventListener('submit',event=>{event.preventDefault();if(!quote.reportValidity())return;const trap=quote.querySelector('[name=website]');if(trap?.value)return;const ref=`TRI-${new Date().toISOString().slice(2,10).replaceAll('-','')}-${Math.random().toString(36).slice(2,6).toUpperCase()}`;const result=document.querySelector('#form-result');result.hidden=false;result.innerHTML=`<strong>Project details prepared.</strong><br>Reference: ${ref}<br>Your details have not been transmitted in this preview. Once [EMAIL], the secure form service and spam protection are connected, Trident will receive the enquiry and you will receive an email copy.`;result.focus()});
const banner=document.querySelector('.cookie-banner');if(localStorage.getItem('trident-cookie-choice'))banner?.remove();document.querySelectorAll('[data-cookie]').forEach(button=>button.addEventListener('click',()=>{localStorage.setItem('trident-cookie-choice',button.dataset.cookie);banner?.remove()}));
